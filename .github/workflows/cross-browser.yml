name: Cross-Browser Tests

on:
  schedule:
    # Run every Monday and Friday at 6 AM UTC
    - cron: '0 6 * * 1,5'
  workflow_dispatch:  # Allow manual triggering

jobs:
  cross-browser-tests:
    name: ${{ matrix.browser }} Browser Tests
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]
        python-version: ['3.11']
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      
      - name: Install Playwright browsers
        run: |
          playwright install ${{ matrix.browser }}
          playwright install-deps ${{ matrix.browser }}
      
      - name: Run smoke tests on ${{ matrix.browser }}
        run: pytest -m smoke -v --browser=${{ matrix.browser }}
        continue-on-error: true
      
      - name: Run all tests on ${{ matrix.browser }}
        if: always()
        run: pytest -v --browser=${{ matrix.browser }} --html=report-${{ matrix.browser }}.html --self-contained-html
        continue-on-error: true
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cross-browser-results-${{ matrix.browser }}
          path: report-${{ matrix.browser }}.html
          retention-days: 30
